#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int add(int a, int b)
{
int a = 89;
int b = 9;
    int n;

    n = (a + b);

    printf("%d\n", n);

    return (n);
}
